---
name: version-bump
description: Use only when explicitly invoked as $version-bump for this repository's Rust package release version bump workflow. This Codex skill delegates to the canonical Claude skill in .claude/skills/version-bump/SKILL.md.
---

# Version Bump

Use this skill only after explicit `$version-bump` invocation.

Read and follow the canonical workflow in `.claude/skills/version-bump/SKILL.md`.

